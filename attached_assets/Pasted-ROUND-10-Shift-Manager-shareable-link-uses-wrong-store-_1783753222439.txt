ROUND 10 — Shift Manager shareable link uses wrong store
Context: The "Share with Crew" link on the Shift Manager page always shows the first store's URL, even when a different store is selected in the store switcher. The backend already resolves /api/shifts/public/:username per store correctly — this is a frontend-only fix in one file.
The fix — modify ONLY client/src/pages/ShiftsManager.tsx:

Add the import: import { useStore } from "@/contexts/StoreContext";
Inside the ShiftsManager component, add: const { activeStore } = useStore();
In copyLinkToClipboard (~line 464): change the guard from if (!user?.shopName) to if (!activeStore?.shopName), and change the link to:
const shareableLink = \window.location.origin/{window.location.origin}/
window.location.origin/{encodeURIComponent(activeStore.shopName)}/shifts\`;`
In the readonly Input (~line 555): change the value to:
activeStore?.shopName ? \window.location.origin/{window.location.origin}/
window.location.origin/{encodeURIComponent(activeStore.shopName)}/shifts\` : ""`
user from useAuth() is now unused in this file — remove the const { user } = useAuth(); line and the useAuth import.

HARD NO-TOUCH LIST — do not modify any of the following:

server/routes.ts or any server file (the public shifts route is correct as-is)
client/src/pages/PublicShifts.tsx
client/src/contexts/StoreContext.tsx
client/src/lib/queryClient.ts
client/src/App.tsx routing
shared/schema.ts, drizzle.config.ts, package.json, package-lock.json, .env
Anything related to Stripe, Telegram, SSE, or n8n
Any other page or component — this fix touches exactly one file

Definition of done:

 npm run build passes with zero errors
 git diff shows changes in client/src/pages/ShiftsManager.tsx only
 With Store A active, the Share with Crew input and Copy Link both produce /{storeA-shopName}/shifts
 Switch to Store B in the store switcher (no page reload needed) — the input and copied link immediately show /{storeB-shopName}/shifts
 Opening Store B's link and entering Store B's PIN shows Store B's shifts only

Report back with the diff summary when done.